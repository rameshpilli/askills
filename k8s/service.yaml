apiVersion: v1
kind: Service
metadata:
  name: claude-agent-skills
  labels:
    app: claude-agent-skills
spec:
  type: ClusterIP
  selector:
    app: claude-agent-skills
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
