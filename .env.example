# Claude Agent SDK Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# API Credentials (one of these is required)
# =============================================================================

# Option 1: Direct Anthropic API key
ANTHROPIC_API_KEY=your_api_key_here

# Option 2: Corporate LLM Gateway
# ANTHROPIC_BASE_URL=https://your-gateway.example.com/v1
# or
# LLM_GATEWAY_URL=https://your-gateway.example.com/v1

# =============================================================================
# Corporate Gateway Settings
# =============================================================================

# Custom headers for gateway authentication (JSON format)
# Example: {"X-Api-Key": "your-key", "X-Tenant-Id": "your-tenant"}
# LLM_GATEWAY_HEADERS={"Authorization": "Bearer your-token"}

# Model name override (if gateway uses different model names)
# CLAUDE_MODEL=claude-3-sonnet

# =============================================================================
# Agent Settings
# =============================================================================

# Working directory for the agent (default: /app in Docker)
# CLAUDE_AGENT_CWD=/app

# Skills source directory (default: /app/skills in Docker)
# CLAUDE_AGENT_SKILLS_DIR=/app/skills

# =============================================================================
# Server Settings
# =============================================================================

# HOST=0.0.0.0
# PORT=8080
