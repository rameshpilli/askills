# Claude Agent SDK Configuration
# Copy this file to .env and fill in your values

# Required: Anthropic API key
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Custom LLM gateway URL (for internal deployments)
# LLM_GATEWAY_URL=https://your-gateway.example.com

# Optional: Working directory for the agent (default: /app in Docker, current dir locally)
# CLAUDE_AGENT_CWD=/app

# Optional: Skills source directory (default: /app/skills in Docker, ./skills locally)
# CLAUDE_AGENT_SKILLS_DIR=/app/skills

# Optional: Server configuration
# HOST=0.0.0.0
# PORT=8080
